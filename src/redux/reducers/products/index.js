const initialState = {
  products: [],
  wishlist: [],
  cart: [],
  productDetail: {},
  params: {},
  totalProducts: 0,
};

const tiendaProductsReducer = (state = initialState, action) => {
  switch (action.type) {
  }
};
